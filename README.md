# PouchCare Platform

- Build the WordPress MVP from `mvp/` (theme, builder, template packs)
- Track rollout status in `mvp/PROGRESS.md`
- Enterprise release checks in `enterprise/docs/`
- Marketing / documentation site (React + Vite): `apps/pouchcare-frontend/` — run `npm install` then `npm run dev` from that folder
