# Management app — tracked work / QA checklists

Use these markdown checklists so invoice, finance, and admin features are not dropped between iterations.

| Document | Purpose |
|----------|---------|
| [InvoicePrintPdf-rollout.md](./InvoicePrintPdf-rollout.md) | Invoice detail print + PDF export, API field mapping, and follow-ups (line items, list print, E2E). |

When adding a new area, create `FeatureName-rollout.md` here and add a row to this table.
