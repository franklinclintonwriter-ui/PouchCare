# Baked into `vite build` for Cloudflare Pages / static hosting.
# Override locally with `.env.production.local` (gitignored via `.env.*.local`).
VITE_API_URL=https://api.pouchcare.com
VITE_WS_URL=wss://api.pouchcare.com/v1/realtime
